module.exports = {
  plugins: [
    {
      resolve: `gatsby-theme-blog`,
      options: {
        // basePath defaults to `/`
      },
    },
  ],
  siteMetadata: {
    title: `Hello, Gatsby!`,
    social: {},
  },
};
